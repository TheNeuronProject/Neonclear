// Import basic elements
import tpl from './tpl.ef'
import style from './style.css'
import styled from '../../utils/styled.js'

const Article = styled(tpl, style)

// Export the module
export { Article }
